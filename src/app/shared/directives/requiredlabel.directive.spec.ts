import { RequiredLabelDirective } from './requiredlabel.directive';

describe('RequiredLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new RequiredLabelDirective();
    expect(directive).toBeTruthy();
  });
});
